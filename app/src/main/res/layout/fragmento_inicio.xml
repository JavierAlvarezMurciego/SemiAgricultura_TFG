<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Guideline para controlar la altura -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.55"/>

    <!-- Carrusel de noticias ocupa la mitad superior -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerNoticias"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/guideline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="20dp"/>

    <!-- Los Indicadores para los puntos -->
    <LinearLayout
        android:id="@+id/puntitos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/viewPagerNoticias"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/bCultFav"
        android:layout_marginBottom="8dp"/>

    <!-- Botón añadir favoritos -->
    <Button
        android:id="@+id/bCultFav"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Añadir Cultivo Favorito"
        android:textColor="@android:color/black"
        android:backgroundTint="#FFEB3B"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        app:layout_constraintTop_toBottomOf="@id/puntitos"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/recyclerViewCultivosFavoritos"
        android:layout_marginBottom="8dp"/>

    <!-- Lista de cultivos favoritos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCultivosFavoritos"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#6179FF"
        app:layout_constraintTop_toBottomOf="@id/bCultFav"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.4"
        android:padding="8dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>